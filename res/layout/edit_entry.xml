<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res/com.DGSD.SecretDiary"
      android:layout_width="fill_parent"
      android:layout_height="fill_parent"
      android:orientation="vertical">

    <com.viewpagerindicator.TitlePageIndicator
        android:id="@+id/indicator"
        android:padding="20dip"
        android:layout_height="wrap_content"
        android:layout_width="fill_parent"
        android:background="@color/pager_indicator_background"
        app:footerColor="@color/pager_indicator_line"
        app:footerLineHeight="3dp"
        app:footerIndicatorStyle="underline"
        app:textColor="@color/pager_indicator_text"
        app:selectedColor="@color/pager_indicator_selected_text"
        app:selectedBold="false"
        />

    <android.support.v4.view.ViewPager
        android:id="@+id/pager"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"/>
</LinearLayout>